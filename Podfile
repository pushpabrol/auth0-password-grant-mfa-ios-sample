# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'
use_frameworks!

target 'Auth0Mfa' do
   pod "Auth0", '1.5'
   pod "SimpleKeychain"
   pod "JWTDecode"
   pod "Alamofire"
end
